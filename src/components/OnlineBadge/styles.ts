import { createStyles } from '@material-ui/core';

export const OnlineBadgeStyle = createStyles({
  badge: {
    backgroundColor: '#068206',
    position: 'absolute',
    marginRight: '5px',
    marginBottom: '5px',
    boxShadow: '0px 16px 48px rgba(0, 0, 0, 0.08)',
    border: '1px solid #FFFFFF',
    width: '10px',
    height: '10px',
    borderRadius: '50%',
  },
});
